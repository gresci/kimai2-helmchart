apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "a-pack-source.fullname" . }}-test-connection"
  labels:
    {{- include "a-pack-source.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "a-pack-source.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
