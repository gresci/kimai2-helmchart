apiVersion: v1
kind: Service
metadata:
  name: {{ include "a-pack-source.fullname" . }}
  labels:
    {{- include "a-pack-source.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "a-pack-source.selectorLabels" . | nindent 4 }}
